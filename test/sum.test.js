import sum from '../code/string/reverse'
test('reverse world', () => {
  // eslint-disable-next-line jest/valid-expect
  expect(sum("Let's take LeetCode contest")).toBe("s'teL ekat edoCteeL tsetnoc")
})
